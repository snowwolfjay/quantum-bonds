# 修复组件导入和翻译问题

## 问题分析

通过检查代码库，我发现了以下问题：

### 1. 组件使用但未导入的问题
- **QuickCalcComponent.vue**: template中使用了`<ion-label>`（第42行），但script中未导入`IonLabel`
- **PreciseCalcComponent.vue**: template中使用了`<ion-label>`（第42行），但script中未导入`IonLabel`
- **MainCalcPage.vue**: template中使用了`<ion-label>`（第10、13行），但script中未导入`IonLabel`
- **PersonInputComponent.vue**: template中使用了`<ion-footer>`和`<ion-toolbar>`（第81-88行），但script中未导入`IonFooter`

### 2. 翻译文件问题
- 经过检查，中英文翻译文件（zh.json和en.json）是完整的，所有键都存在

## 修复计划

### 1. 修复QuickCalcComponent.vue
- 在script的import语句中添加`IonLabel`导入

### 2. 修复PreciseCalcComponent.vue
- 在script的import语句中添加`IonLabel`导入

### 3. 修复MainCalcPage.vue
- 在script的import语句中添加`IonLabel`导入

### 4. 修复PersonInputComponent.vue
- 在script的import语句中添加`IonFooter`和`IonToolbar`导入

## 验证计划

- 修复后运行项目，检查是否有编译错误
- 检查组件是否能正常显示
- 测试语言切换功能是否正常
- 测试计算功能是否正常

## 修复步骤

1. 编辑`src/components/QuickCalcComponent.vue`，添加缺失的`IonLabel`导入
2. 编辑`src/components/PreciseCalcComponent.vue`，添加缺失的`IonLabel`导入
3. 编辑`src/views/MainCalcPage.vue`，添加缺失的`IonLabel`导入
4. 编辑`src/components/PersonInputComponent.vue`，添加缺失的`IonFooter`和`IonToolbar`导入
5. 运行项目进行验证